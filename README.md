# EML 计算台

第一版本是一个本地符号计算小程序，以 `EML(x, y) = e^x - ln(y)` 为基础，从初始值 `1` 逐步生成和保存新数值。

直接打开 `dist/eml-workbench.html` 即可使用。

## 当前能力

- 符号计算与基础公式化简
- 数值和公式双重去重
- 同一数值保存多个公式来源
- 直接公式和完整计算树
- 初始值 `1` 保护
- 本地缓存、JSON 保存与导入
- 鼠标和触摸拖放、选择后点击输入

需求、设计、实施计划和测试报告位于 `doc` 目录。
