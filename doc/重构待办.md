# EML 重构待办

## P0：稳定基线

- [x] 保存 `v1.0.0` 阶段性总结。
- [x] 确认 71 项单元测试全部通过。
- [x] 生成并校验 `EML-v1.0.0.zip`。
- [x] 创建并推送 Git 标签 `v1.0.0`。

## P1：公式引擎模块化

- [ ] 把定义域和性质证明从公式重写中拆出。
- [ ] 按基础代数、指数、对数、复数和三角函数拆分规则模块。
- [ ] 建立统一规则注册表，规则元数据与执行逻辑放在同一位置。
- [ ] 保持规则执行顺序显式可查，并增加顺序回归测试。
- [ ] 保持 47 条现有规则的输出和步骤记录兼容。

## P1：计算树性能

- [ ] 将完整计算树改为按需展开，而不是首次选择时全部生成。
- [ ] 对共享上游节点使用引用或缓存，避免重复构建相同分支。
- [ ] 增加最大节点数和最大展开深度保护。
- [ ] 增加深树、宽树、共享分支和循环数据测试。

## P1：数据安全

- [ ] 为每种表达式节点增加严格结构校验。
- [ ] 限制常量名称、整数类型和有限数值范围。
- [ ] 校验推导记录、原始表达式和化简步骤。
- [ ] 限制导入文件大小、数值数量、字符串长度和树深。
- [ ] 保持现有 V2 文件兼容；如需变更则提供明确迁移路径。

## P2：化简状态

- [ ] 将化简轮数上限定义为具名配置。
- [ ] 达到上限时在界面显示提示并禁用“添加”。
- [ ] 增加规则循环和上限触发测试。

## P2：界面职责

- [ ] 从 `app.js` 拆分计算区、数值栏、计算树和文件操作。
- [ ] 避免输入变化时重建整个数值栏和完整计算树。
- [ ] 保持拖放、滚轮、缩放和键盘操作行为不变。

## P2：测试结构

- [ ] 将单元测试按 expression、rules、store、persistence 和 viewport 拆分。
- [ ] 建立每条规则至少一项直接命中测试的覆盖检查。
- [ ] 增加自动浏览器交互测试，覆盖 I01-I38 的关键流程。
- [ ] 为发布包增加构建后冒烟测试和校验和检查。

## 验收条件

- [ ] 全部既有单元测试继续通过。
- [ ] 关键交互测试自动化并通过。
- [ ] 保存文件可在重构版中无损导入。
- [ ] 深层计算树不再一次性生成全部 DOM。
- [ ] 公式规则可按模块独立测试和审计。
- [ ] 重新生成的单文件版本可直接打开运行。
