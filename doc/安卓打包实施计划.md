# 安卓打包实施计划

- 版本：v1.2.0
- 分支：`feat/android-apk`
- 上游：`main` @ `0ae124e`
- 依据：`doc/安卓打包方案.md`、`doc/安卓打包需求与测试用例.md`
- 规范：`build-tested-mini-app`（每阶段：设计 → 改代码 → 测试 → 提交）

## 阶段划分

### 阶段 0：规划 ✅

- [x] `doc/安卓打包方案.md`（架构、分支策略、鸿蒙版本结论、风险）
- [x] `doc/安卓打包需求与测试用例.md`（U01–U20）
- [x] `doc/安卓打包实施计划.md`（本文件）
- [x] 分支 `feat/android-apk` 已创建并切入
- [x] 应用身份确认：包名 `com.xiaoxuhui.eml` / 名称「EML 计算台」/ minSdk 24

### 阶段 1：Android 工程骨架

- [ ] `android/settings.gradle.kts`
- [ ] `android/build.gradle.kts`
- [ ] `android/gradle.properties`
- [ ] `android/gradle/wrapper/gradle-wrapper.properties`
- [ ] `android/app/build.gradle.kts`（applicationId、minSdk 24、targetSdk 34、versionCode 2）
- [ ] `android/app/src/main/AndroidManifest.xml`
- [ ] `android/app/src/main/java/com/xiaoxuhui/eml/MainActivity.kt`
- [ ] `android/app/src/main/res/values/themes.xml`
- [ ] `android/app/src/main/res/mipmap-*/ic_launcher.png`（自适应图标）
- [ ] `android/app/src/main/res/xml/backup_rules.xml`（可选）
- [ ] `android/.gitignore`（`build/`、`.gradle/`、`local.properties`、`*.apk`、`*.aab`）
- [ ] `android/README.md`（如何本地构建）

提交：`feat: 搭建 Android WebView 工程骨架`

### 阶段 2：网页资源同步

- [ ] `scripts/sync-android-assets.mjs`（dist → android assets）
- [ ] `package.json` 增加 `sync:android` 脚本
- [ ] 校验：同步后 assets 内 html 与 dist 一致（U03）

提交：`feat: 新增网页资源同步脚本`

### 阶段 3：WebView 适配（基础可用性）

- [ ] 启用 JavaScript、domStorage（U13）
- [ ] 禁用长按放大/文本选择（避免误触）
- [ ] 处理返回键（U20）
- [ ] 加载 assets 内本地页面（U09 离线）
- [ ] 屏幕常亮（可选）
- [ ] 文件导入/导出的 WebView 支持（U14，视可行性）

提交：`feat: WebView 适配与离线加载`

### 阶段 4：GitHub Actions 云构建

- [ ] `.github/workflows/android-apk.yml`
- [ ] 触发条件：`android/**` 变更或手动 `workflow_dispatch`
- [ ] 步骤：JDK 17 → Gradle 构建 → 上传 APK 产物
- [ ] 打 tag 时自动挂到 Release（可选）

提交：`ci: 新增安卓 APK 云构建 workflow`

### 阶段 5：出包与验证

- [ ] 首次构建产出 APK
- [ ] 校验产物大小、结构（U01–U04）
- [ ] 下载 APK 校验 assets 内容 md5（U03）

提交：`release: 构建 v1.2.0 安卓 APK`

### 阶段 6：真机测试

- [ ] 安卓设备安装启动（U05、U07、U08）
- [ ] 鸿蒙 4.x（Mate 50 Pro）安装启动（U06）
- [ ] 离线计算、公式详情、计算树（U09–U12）
- [ ] 数据持久化、导入导出（U13、U14）
- [ ] 切后台恢复（U15）
- [ ] 竖屏/横屏/旋转/返回键（U16–U20）
- [ ] 产出 `doc/安卓版本测试报告-v1.2.0.md`

提交：`docs: 安卓版本测试报告 v1.2.0`

### 阶段 7：合并与发布

- [ ] PR 合并回 `main`（squash merge）
- [ ] CHANGELOG 增加 v1.2.0 条目
- [ ] 打 tag `v1.2.0`

## 提交纪律

- 每个阶段一个 commit，前缀 `feat` / `fix` / `docs` / `ci` / `release`
- 每阶段结束跑 `node --test tests/*.test.js` 与 `node scripts/build.mjs`
- 不在 `feat/android-apk` 上修改 EML 主应用源码（主应用改动放 `feat/touch-responsive`）

## 环境注意事项（本机已确认）

- 无 JDK / Android SDK / Gradle → 不本机构建，走 GitHub Actions
- C 盘空间紧张 → 不安装安卓工具链
- git 嵌套分支在本环境需写入 `packed-refs`（`git branch feat/xxx` 会静默失败）
- bash 环境下 `git checkout` 对 `.git` 有破坏性 → 一律用 `symbolic-ref` + `reset --mixed` 切分支
